apiVersion: v1
kind: Service

metadata:
  name: django-auth
  namespace: django-auth

spec:
  selector:
    app: django-auth

  ports:
    - name: http
      port: 80
      targetPort: 8000

  type: ClusterIP